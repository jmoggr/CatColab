name: ci

on:
  push:
    branches:
      - main
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Encode Secret in Base64
        run: |
          echo -n "${{ secrets.NETLIFY_SITE_ID }}" | base64
